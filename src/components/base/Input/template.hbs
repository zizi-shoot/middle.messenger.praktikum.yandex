<!--suppress HtmlFormInputWithoutLabel -->
<input
  id="input-{{name}}"
  name="{{name}}"
  type="{{type}}"
  class="{{classList}}"
  placeholder="{{placeholder}}"
  value="{{value}}"
  {{#if autocompleteOff}}
  autocomplete="off"
  {{/if}}
/>
